server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: bike-service
          uri: https://github.com/AndreG0/microservices
          search-paths: config-data
  application:
    name: config-service
